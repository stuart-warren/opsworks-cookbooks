# Input for syslog logs preformed in JSON - To lower requirements for parsing

input {
    tcp {
        type => "tcp-json"
        port => 1516
        codec => "json_lines"
        tags => ["json"]
    }
}
